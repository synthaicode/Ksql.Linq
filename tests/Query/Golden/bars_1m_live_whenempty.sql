create table bar_1m_live with(kafka_topic='bar_1m_live', key_format='avro', value_format='avro')as select windowstart as windowstartraw, broker as broker, symbol as symbol, windowstart as bucketstart, earliest_by_offset(o.open)as open, max(o.high)as high, min(o.low)as low, latest_by_offset(o.ksqltimeframeclose)as ksqltimeframeclose from bar_1s_rows o window tumbling(size 1 minutes)group by broker, symbol emit changes;
