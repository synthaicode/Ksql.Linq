create table if not exists keypath_none with(kafka_topic='keypath_none', key_format='avro', value_format='avro')as select broker as broker, symbol as symbol, sum(qty)as total from tableentity group by broker, symbol emit changes;